apiVersion: v1
kind: Service
metadata:
  name: redis-server-svc
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: redis-server
  ports:
    - protocol: TCP
      port: 6379
      targetPort: {{ .Values.redis.containerPort }}
